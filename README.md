# unity-ballistic-launch-system
This project implements a ballistic launch simulation in Unity3D. The system calculates the required launch angles (horizontal and vertical) to hit a target based on the initial position and target coordinates. After computing the trajectory parameters, the projectile flight is simulated using Unity physics.
